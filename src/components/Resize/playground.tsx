import Resize from './resize'

export const meta = {
  description: 'A draggable divider that splits two panels with elastic resistance near the edges.',
}

export default function ResizePlayground() {
  return <Resize />
}
