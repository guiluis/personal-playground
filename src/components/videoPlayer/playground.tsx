import VideoPlayer from './videoPlayer'

export const meta = {
  description: 'A minimal video player with custom controls and smooth playback interactions.',
}

export default function VideoPlayerPlayground() {
  return <VideoPlayer />
}
