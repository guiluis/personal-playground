import { CursorFollowing } from './cursorFollowing'

export const meta = {
  description: 'An element that smoothly follows the cursor position with spring physics.',
}

export default function CursorFollowingPlayground() {
  return <CursorFollowing />
}